# CLAUDE.md

React(TS) / Hono の学習を目的とした勤怠管理アプリ。
仕様は [docs/spec.md](docs/spec.md) を参照し、仕様を変更したら spec.md も必ず更新する。

## 作業の進め方

**作業は STEP 単位で進め、各 STEP を「提案 → 承認 → 実装 → ログ」の順で進める。**

- STEP の一覧と進捗は [docs/steps.md](docs/steps.md) で管理する
- ログは `docs/logs/step-NN-<slug>.md` に作成する
- ログのテンプレートは [docs/logs/TEMPLATE.md](docs/logs/TEMPLATE.md)

## 1. 着手前に提案し、承認を得る

**いきなり実装を始めない。** 何をやるつもりかを先に提案し、承認を得てから手を動かす。

提案に含めること:

| 項目 | 内容 |
|---|---|
| やること | この STEP で作るもの・変更するもの |
| 方針と理由 | 採用する設計・実装方針と、なぜそれを選ぶのか |
| 対案 | 他に考えられる選択肢と、外す理由。**判断が分かれるものは選択肢として提示し、決めてもらう** |
| 影響範囲 | 新規作成・変更するファイル |
| 完了の定義 | 何ができたら完了とするか |

- 承認された範囲の中では、逐一確認せずに進めてよい
- 途中で**承認された範囲を超える**必要が出たら、いったん手を止めて再提案する
- [docs/spec.md](docs/spec.md) の変更、および過去の設計判断を覆す場合も、必ず提案して承認を得る

例外: ユーザーが具体的に指示した単発の修正（typo、リンク切れ、指定された小さな変更）は、
指示自体が承認にあたるため、提案せずそのまま実行してよい。

## 2. 実装する

承認された範囲を実装する。STEP の完了条件は、原則として
**テストが通り、ブラウザで動作を確認できること**。

## 3. ログを書く

STEP を終える前にログを書く。あとでまとめて書かない（つまづきの詳細が失われるため）。

### ログに必ず書くこと

ログの目的は「あとから読んで、判断を再現・検証できること」。作業の実況ではない。

| 項目 | 内容 |
|---|---|
| 目的 | この STEP で何を達成するのか、なぜこのタイミングなのか |
| やったこと | 実際に作ったもの・変更したもの |
| なぜそうしたか | 採用した判断の理由。**採用しなかった選択肢と、外した理由も書く** |
| つまづき | エラー内容・原因・解決方法。**解決までに試して外れた仮説も残す** |
| 気づき・学び | 今後に効く知見。ライブラリの挙動、設計の落とし穴、言語仕様の罠 |
| 次への申し送り | 未完了事項、先送りした判断 |

**つまづきと気づきは省略しない。** うまくいった手順より、詰まった箇所の方が後から価値が出る。
「特になし」と書くくらいなら、なぜ詰まらなかったのかを一行書く。

### 判断が変わったとき

過去の判断を覆した場合は、**元の判断 / 覆した理由 / 新しい判断** の3点をログに残す。
結論だけを書き換えると、同じ検討を将来もう一度やることになる。

spec.md 側は新しい結論だけを書き、経緯はログに置く。
（spec.md = 現在の正、logs = そこに至った経緯、という役割分担）

## 4. コミットする

**作業の区切りごとにコミットする。** STEP の完了時だけでなく、STEP の中でも
意味のあるまとまりができたらその都度コミットする（スキーマ → API → テスト、など）。

コミット運用は承認済みのため、区切りごとに都度確認は取らない。
ブランチは切らず `main` に直接コミットする。

### コミットメッセージ

**1〜2 行に収める。**

- 1 行目は `<種別>: <要約>`。種別は `feat` / `fix` / `docs` / `test` / `chore` / `refactor`
- 補足が要る場合のみ、空行を挟んで 1 行だけ書く
- **詳細は作業ログに書くので、コミットメッセージには書かない**
- 日本語で書く
- 末尾に付く `Co-Authored-By` 行は行数に数えない

```
feat: 打刻APIと状態遷移バリデーションを追加
docs: 申請テーブルをヘッダ/明細構造に変更
```
